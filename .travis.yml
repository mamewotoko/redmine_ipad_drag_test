sudo: required
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
services:
  - docker
python:
  - "2.7"
before_install:
  - sudo pip install selenium mechanize
script:
  - ./init.sh
  - ./run.sh
  - ./finalize.sh
