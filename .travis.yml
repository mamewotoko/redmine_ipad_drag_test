sudo: required
language: python
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
services:
  - docker
python:
  - "2.7"
install:
  - pip install --user selenium mechanize
  - pip list --user
script:
  - ./init.sh
  - ./run.sh
  - ./finalize.sh
